-- Retrieve the total number of orders placed.

SELECT 
    COUNT(order_id) AS total_orders
FROM
    orders;
    
select avg(pizzas.price) from dominos.pizzas;

select max(pizzas.price) from dominos.pizzas;
    
select min(pizzas.price) from dominos.pizzas;

    
    
    